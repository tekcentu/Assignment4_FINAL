"""
Framework-neutral helpers shared by GUI frontends.

This package contains code that does not depend on any GUI toolkit:
the undoable command stack (``commands``), the ``.txt`` writer used by
the GUIs (``file_writer``), and the results text formatter
(``results_view``).
"""
